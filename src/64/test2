bits 64
global print
section .data
msg db "helloWorld", 11
section .text
print:
mov rdi, 1 
lea rsi, [rel msg]
mov rdx, 10 
mov rax, 1
syscall
mov rax, old_entrypoint
jmp rax
    
msg:
db , "helloWorld"